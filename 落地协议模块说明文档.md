# 落地协议模块 · 使用与部署说明文档

**项目**：Momenta LPA 可视化工作平台（reception-next）  
**模块**：落地协议追踪与对赌管理  
**协议背景**：魔门塔（苏州）与苏州高铁新城管委会，2024–2028 年商务合作协议  
**线上地址**：https://reception-next.vercel.app  
**文档版本**：2026-07-22

---

## 一、模块整体说明

落地协议模块服务于一个核心目标：**将协议里所有的考核要求数字化，让每个人随时都能看到"现在完成了多少、离拿到补贴还差多少"**。

模块分为三个入口，分工明确：

| 入口 | 网址 | 谁在用 | 做什么 |
|---|---|---|---|
| **落地协议** | `/landing` | 所有人 | 查看 KPI 进度、定性义务状态、协议文件 |
| **数据中台** | `/data-center` | 财务 / HR / 研发负责人 | 每月录入原始数据 |
| **数据大屏** | `/screen` | 汇报场合 | 全屏可视化，用于向管委会或管理层展示 |

**三者的关系**：数据中台是"入口"，每月录入数据后，落地协议页和数据大屏会立刻自动刷新，不需要人工导出或手动汇总。

---

## 二、落地协议主页（`/landing`）

### 2.1 页面顶部横幅

打开页面首先看到深色横幅，显示当前年度的整体履约情况：

- **综合履约分**：0–100 分的圆环图，由 7 项 KPI 按权重加权计算得出。分数越高代表履约越好，对应补贴获取能力越强。
- **状态徽章**：当前年度 7 项 KPI 分别有多少项达标、打折、风险、待录入，用颜色区分一目了然。
- **年份切换**：右上角下拉框切换 2024–2028 任意年份查看对应目标和进度。
- **倒计时**：距当年 12 月 31 日考核截止的天数，不足 90 天时变红提示。
- **录入数据按钮**：直接跳转到数据中台，方便在看到某项 KPI 进度不对时立刻去补录数据。
- **数据大屏按钮**：一键进入汇报模式。

### 2.2 Tab 一：量化 KPI 追踪

**7 张 KPI 卡片横排**，每张卡片展示一项指标的当前状态：

- 指标名称和单位（亿元 / 人 / 件 / 家）
- 当年 YTD 实际值 vs. 年度目标
- 完成率进度条，颜色编码：
  - **绿色（≥ 90%）**：可获全额补贴
  - **黄色（70%–90%）**：按比例折扣拨付
  - **红色（< 70%）**：当年零补贴
  - **灰色**：该指标本年度尚未录入数据
- 距离 90% 全额补贴线还差多少的具体数字

**点击任意 KPI 卡片**，下方展开该指标的五年阶梯对赌详情：
- 逐年列出 2024–2028 年的目标值
- 当前年度显示实际进度条（进度条内有 90% 参考竖线）
- 未来年份仅展示目标值，灰色显示

**页面底部**还有一个「全额补贴缺口汇总」面板，把所有未达标 KPI 的差距集中显示，方便判断下一步重点补足哪项。

### 2.3 Tab 二：定性义务合规

展示协议中 12 条非量化义务（无法用数字衡量，只能标注状态）：

- 顶部有合规率进度条，显示 12 条中有几条已合规
- 每条义务的状态分为四档：**已合规 / 进行中 / 待处理 / 存在风险**
- 每条显示：义务名称、对应协议条款编号、当前状态、负责人填写的状态说明、已上传的支撑材料（PDF/图片链接）
- 有编辑权限的用户可以点击「编辑」修改状态、填写说明、上传支撑材料附件

### 2.4 Tab 三：协议文件

协议相关文件的统一管理入口：

- 文件分为五类：**协议原文 / 补充协议 / 审计报告 / 年度报告 / 其他**，顶部可按分类筛选
- 有编辑权限的用户可以上传文件，点击「查看」在浏览器内预览，点击「下载」下载到本地
- 文件直接存储在数据库中，不依赖 Supabase，上传后立即可用
- 单文件大小限制 10MB，支持 PDF、Word、Excel、图片等常见格式

---

## 三、数据中台（`/data-center`）

### 3.1 核心逻辑

数据中台是落地协议模块的"数据来源"。财务、HR、研发负责人每月在这里录入当月原始数据，系统自动汇总成 KPI 进度。**不录数据，落地协议页就显示"待录入"，无法判断状态。**

### 3.2 月份选择

页面顶部有月份选择器，默认显示上一个自然月。切换月份后，表单中自动填入该月已有的历史数据（如果有的话），方便核对或修改。同时会显示上月数据作对比，若本月输入值与上月不同，字段旁会显示橙色变化提示。

### 3.3 三个录入分类

**经营与财务（Finance）**

由财务负责人每月录入，数据为月度增量（当月实际发生金额）：

| 字段 | 单位 | 关联哪项 KPI |
|---|---|---|
| 营业收入 | 亿元 | 营业收入 KPI |
| 其中：苏州确认收入 | 亿元 | 参考（验证 ≥60% 是否在苏州） |
| 增值税实缴苏州 | 万元 | 综合税收 KPI（与下一行之和） |
| 企业所得税实缴苏州 | 万元 | 综合税收 KPI（与上一行之和） |
| 个人所得税苏州代扣 | 万元 | 个税金额 KPI |
| 增值税应缴（全额） | 万元 | 参考数据 |
| 企业所得税应缴（全额） | 万元 | 参考数据 |
| 研发投入 | 万元 | 高企申报参考 |

> 综合税收由系统自动计算，无需手填：增值税实缴苏州 + 企业所得税实缴苏州，并实时显示万元和亿元换算结果。

**人才与团队（HR）**

由 HR 负责人每月录入，数据为截至当月末的累计值：

| 字段 | 单位 | 关联哪项 KPI |
|---|---|---|
| 苏州社保参保人数 | 人 | 社保人数 KPI |
| 国家级人才申报人数 | 人 | 国家级人才 KPI |
| 已引进产业链企业数 | 家 | 产业链引进 KPI |
| 核心岗位苏州劳动关系 | 人 | 参考（300人义务） |
| 其中：高管人数 | 人 | 参考 |
| 年薪50万以上员工数 | 人 | 个税奖励计算参考 |

**研发与知识产权（IP）**

由研发/知产负责人录入，数据为截至当月末的累计件数：

| 字段 | 单位 | 关联哪项 KPI |
|---|---|---|
| 发明专利申请（累计） | 件 | 发明专利申请 KPI |
| 发明专利授权（累计） | 件 | 高企申报参考 |
| 实用新型专利（累计） | 件 | 高企申报参考 |
| 软件著作权（累计） | 件 | 高企申报参考 |

### 3.4 自定义字段显示

每个录入 Tab 右上角有「配置字段」按钮。点击后进入配置模式，每个字段右边出现一个开关：

- **关闭某字段**：该字段灰显不可输入，保存时也不写入
- **开启某字段**：正常显示和保存
- 配置保存在浏览器本地（localStorage），不影响其他人的视图，刷新后保留
- 建议：不需要的参考字段可以隐藏，保持界面简洁；但标有「必填」标记的 KPI 相关字段尽量保留

### 3.5 保存规则

- 每个 Tab 独立保存，点击该 Tab 底部的「保存」按钮
- 同一月份同一分类的数据，第二次保存会**覆盖**之前的数据（不叠加）
- 保存成功后，页面顶部的状态条会显示「已保存 · 时间」
- 保存后立即跳转到落地协议页，即可看到最新 KPI 进度

---

## 四、数据大屏（`/screen`）

用于向管委会或内部管理层汇报时使用。特点：

- **无侧边栏**，全屏展示，更适合投影仪或大屏显示器
- **浅蓝色背景**，视觉清晰，色彩层次分明
- **每 60 秒自动刷新**，右上角有实时时钟
- 顶部可切换年份（2024–2028）
- 右上角「← 返回」回到落地协议管理页

大屏展示内容（从左到右三栏）：
- **左栏**：综合履约分圆环 + KPI 状态统计 + 12 条义务合规环形分布图 + 倒计时 + 协议关键节点
- **中栏**：7 项 KPI 完成率小环形图 + 营业收入月度趋势折线图 + 综合税收/社保人数折线图
- **右栏**：7 项 KPI 进度条详情 + 五年营收柱状图 + 补贴预测三档估算

**进入大屏的三种方式**：
1. 直接访问 `/screen`
2. 侧边栏点击对应菜单
3. 落地协议主页顶部「数据大屏」按钮

---

## 五、KPI 考核规则说明

### 5.1 七项量化 KPI 目标

| KPI 名称 | 2024 | 2025 | 2026 | 2027 | 2028 | 单位 | 考核权重 |
|---|---|---|---|---|---|---|---|
| 营业收入 | 5 | 8 | 12 | 18 | 25 | 亿元 | 40% |
| 综合税收 | 0.30 | 0.50 | 0.70 | 1.00 | 1.50 | 亿元 | 20% |
| 个税金额 | 0.05 | 0.08 | 0.12 | 0.18 | 0.25 | 亿元 | 10% |
| 社保人数 | 200 | 280 | 380 | 500 | 650 | 人 | 15% |
| 国家级人才 | 1 | 2 | 3 | 5 | 8 | 人 | 5% |
| 发明专利申请 | 50 | 80 | 120 | 180 | 250 | 件（累计） | 5% |
| 产业链引进 | 1 | 2 | 3 | 5 | 8 | 家（累计） | 5% |

### 5.2 补贴发放规则

| 完成率区间 | 补贴比例 | 系统状态标签 |
|---|---|---|
| ≥ 90% | 全额补贴 | 绿色「全额达标」|
| 70%–90% | 按比例折扣拨付 | 黄色「打折拨付区」|
| < 70% | 零补贴 | 红色「零补贴风险」|

### 5.3 综合履约分计算

综合履约分 = 各 KPI（完成率 × 权重）之和 × 100，满分 100 分。

举例：若营业收入完成 76%、社保人数完成 84%，其余 KPI 待录入，则：

```
0.76 × 40% + 0.84 × 15% = 0.304 + 0.126 = 0.43 → 综合履约分 43 分
```

> 待录入的 KPI 不参与计算，因此录入数据越多，分数越能反映真实情况。

### 5.4 数据录入与 KPI 的对应关系

| KPI | 数据从哪个 Tab 取 | 取哪个字段 | 取哪几个月 |
|---|---|---|---|
| 营业收入 | 经营与财务 | revenue | 当年各月累加 |
| 综合税收 | 经营与财务 | vatPaidSuzhou + citPaidSuzhou | 当年各月累加，万元转亿元 |
| 个税金额 | 经营与财务 | pitSuzhou | 当年各月累加，万元转亿元 |
| 社保人数 | 人才与团队 | socialInsuranceCount | 取最新一个月的值 |
| 国家级人才 | 人才与团队 | nationalTalentCount | 取最新一个月的值 |
| 发明专利申请 | 研发与知识产权 | inventionPatentApplied | 取最新一个月的累计值 |
| 产业链引进 | 人才与团队 | industryChainCount | 取最新一个月的值 |

---

## 六、定性义务说明

12 条义务均来自协议原文，系统中预置，无需手动添加。每条义务可标注以下四种状态，由有编辑权限的用户在「落地协议 → 定性义务」Tab 中维护：

| 状态 | 含义 |
|---|---|
| 已合规 ✅ | 已满足协议要求，有支撑材料 |
| 进行中 🔄 | 正在推进，尚未完成 |
| 待处理 ⏳ | 尚未启动 |
| 存在风险 🔴 | 可能违反协议条款，需要关注 |

每条义务可以：
- 填写状态说明（描述当前进展）
- 上传支撑材料（PDF、图片等，用于证明合规）

**12 条义务清单**：

| 编号 | 名称 | 协议条款 | 核心要求 |
|---|---|---|---|
| 1 | 总部架构合规 | 第1.1.3条(1) | 集团境内公司均在苏州注册或属 VIE 体系 |
| 2 | 营收归集 | 第1.1.3条(2) | 国内销售收入 ≥ 60% 在苏州确认 |
| 3 | IPO 目标 | 第1.1.3条(3) | 2027-12-31 前完成合格 IPO |
| 4 | 上市架构 | 第1.1.3条(4) | 境外上市须在相城区设外商投资实体 |
| 5 | 核心人员 | 第1.1.3条(5) | 高管+核心成员劳动关系落地苏州 ≥ 300 人 |
| 6 | 产业链主 | 第1.1.3条(6) | 引进上下游合作伙伴落地相城 |
| 7 | 总部大楼 | 第1.1.3条(7) | 2029-12-31 前启动拿地建设 |
| 8 | 专款专用 | 第1.1.3条(8) | 补贴资金用于苏州经营，不得挪用 |
| 9 | 股东减持 | 第1.1.3条(9) | 减持纳税地争取在苏州缴纳 |
| 10 | 持续经营 | 第1.1.3条(10) | 自协议签订起经营不低于 8 年 |
| 11 | 变更告知 | 第1.1.3条(11) | 控股权变更前至少 30 日通知甲方 |
| 12 | 配合审查 | 第1.1.3条(12) | 配合甲方财政、审计部门检查 |

---

## 七、角色与权限

系统有三种角色，登录后根据角色决定可以做什么：

| 操作 | viewer（只读） | editor（编辑） | admin（管理员） |
|---|---|---|---|
| 查看落地协议、KPI 进度 | ✅ | ✅ | ✅ |
| 查看定性义务状态 | ✅ | ✅ | ✅ |
| 查看/下载协议文件 | ✅ | ✅ | ✅ |
| 查看数据大屏 | ✅ | ✅ | ✅ |
| 在数据中台录入数据 | ❌ | ✅ | ✅ |
| 编辑定性义务状态和说明 | ❌ | ✅ | ✅ |
| 上传协议文件和支撑材料 | ❌ | ✅ | ✅ |
| 删除文件 | ❌ | ✅ | ✅ |
| 管理用户账号 | ❌ | ❌ | ✅ |
| 系统初始化（seed） | ❌ | ❌ | ✅ |

**建议账号分配**：
- 财务负责人：editor
- HR 负责人：editor
- 研发/知产负责人：editor
- 管理层查看：viewer
- IT 管理员：admin

---

## 八、部署说明

### 8.1 部署平台

系统部署在 **Vercel**（https://vercel.com），数据库使用 **Neon**（https://neon.tech），两者都有免费套餐可满足本项目需求。

### 8.2 需要提前准备什么

**必须有**：
- Vercel 账号（免费注册）
- Neon 账号（免费注册），并创建好一个 PostgreSQL 数据库，获取连接字符串（Connection String）

**可选但建议有**：
- 如果需要接待照片上传功能：Supabase 账号，创建一个公开的 `photos` bucket
- 如果需要飞书提醒功能：飞书开放平台的机器人 Webhook 地址

### 8.3 Vercel 上需要填写的环境变量

在 Vercel 项目设置 → Environment Variables 中添加以下变量：

| 变量名 | 是否必须 | 说明 | 在哪里获取 |
|---|---|---|---|
| `DATABASE_URL` | ✅ 必须 | Neon 数据库连接串，格式为 `postgresql://user:pass@host/db?sslmode=require` | Neon 控制台 → Connection Details |
| `JWT_SECRET` | ✅ 必须 | 随机字符串，用于加密登录 Token，建议 32 位以上 | 自己生成，如：`momenta-lpa-secret-2024-xxxx` |
| `CRON_SECRET` | ✅ 必须 | 定时任务鉴权密码，任意字符串即可 | 自己设定 |
| `SUPABASE_URL` | 接待照片时需要 | Supabase 项目地址 | Supabase 控制台 → Settings → API |
| `SUPABASE_SERVICE_KEY` | 接待照片时需要 | Supabase service_role 密钥 | 同上，注意不是 anon key |
| `FEISHU_WEBHOOK_URL` | 飞书通知时需要 | 飞书机器人 Webhook | 飞书群 → 机器人设置 |
| `FEISHU_APP_ID` | 飞书导入时需要 | 飞书应用 App ID | 飞书开放平台 → 应用 |
| `FEISHU_APP_SECRET` | 飞书导入时需要 | 飞书应用 App Secret | 同上 |

> **注意**：若暂时不用接待和飞书功能，只用落地协议模块，则只需填 `DATABASE_URL`、`JWT_SECRET`、`CRON_SECRET` 三个变量，系统即可正常运行。

### 8.4 部署步骤（全程在网页操作，无需命令行）

**第一步：Fork 或上传代码**

将代码上传到 GitHub 仓库（公开或私有均可）。

**第二步：在 Vercel 创建项目**

登录 Vercel → New Project → 选择 GitHub 仓库 → Import。

Framework Preset 选 **Next.js**，Build Command 和 Output Directory 保持默认。

**第三步：填写环境变量**

在 Vercel 的 Environment Variables 页面，依次添加上面表格中的变量，全部选择 Production + Preview + Development。

**第四步：点击 Deploy**

Vercel 会自动执行构建，构建过程中会自动连接 Neon 数据库并同步数据表结构（无需手动建表）。

构建成功后，Vercel 会分配一个域名（如 `your-project.vercel.app`），访问该域名即可看到登录页面。

### 8.5 部署完成后的初始化操作

**这一步非常重要，不做的话落地协议的定性义务列表是空的。**

**初始化管理员账号**：系统在首次构建时已通过 seed 脚本写入初始账号。

默认登录账号：
- 用户名：`admin`
- 密码：`admin123`

> **请在首次登录后立即在「用户管理」中修改密码。**

**初始化落地协议义务数据（12条）**：

这步需要用管理员账号调用一次接口，有以下两种方式：

**方式一（推荐）**：用任意 HTTP 工具（Postman、Apifox 等）或浏览器开发者工具：

1. 先登录获取 Token：访问 `POST https://your-domain.vercel.app/api/auth/login`，请求体填 `{"username":"admin","password":"admin123"}`
2. 复制返回的 `token` 字段
3. 调用 `POST https://your-domain.vercel.app/api/agreement/seed`，在 Headers 中加 `Authorization: Bearer <复制的token>`
4. 返回 `{"ok":true,"created":12}` 表示成功

**方式二**：直接在浏览器 Console 中执行（登录后在任意页面按 F12）：

```
fetch('/api/agreement/seed', {method:'POST', headers:{Authorization:'Bearer '+localStorage.getItem('token')}}).then(r=>r.json()).then(console.log)
```

看到 `{ok: true, created: 12}` 即成功。**此操作只需执行一次**，重复执行会自动跳过已有数据。

### 8.6 后续更新部署

代码有修改时，重新推送到 GitHub，Vercel 会自动触发重新部署，数据表结构有变更也会自动同步，无需手动操作。

---

## 九、日常使用流程

### 每月数据录入流程

推荐在每个自然月结束后的 1–5 日内完成当月数据录入。

1. 财务负责人进入「数据中台」→ 选择上个月 → 切换到「经营与财务」Tab → 填写营业收入、税收、个税等数据 → 点击保存
2. HR 负责人进入「数据中台」→ 选择上个月 → 切换到「人才与团队」Tab → 填写社保参保人数、人才数等 → 点击保存
3. 研发负责人进入「数据中台」→ 选择上个月 → 切换到「研发与知识产权」Tab → 更新专利累计数量 → 点击保存
4. 任何人打开「落地协议」主页，KPI 进度已自动更新

### 定性义务维护流程

当义务状态发生变化（例如 IPO 辅导备案完成），由负责人：

1. 进入「落地协议」→「定性义务」Tab
2. 找到对应义务 → 点击「编辑」
3. 修改状态（如从「进行中」改为「已合规」）
4. 填写说明文字
5. 如有证明材料（合同、批文、照片等）可直接上传
6. 点击保存

### 汇报使用流程

向管委会或管理层汇报时：

1. 进入「落地协议」主页 → 点击顶部「数据大屏」按钮
2. 在大屏上切换需要展示的年份
3. 按 F11 全屏展示（或将浏览器标签页全屏）
4. 大屏每 60 秒自动刷新，无需手动操作

---

## 十、常见问题

**Q：数据录入后落地协议页面没有更新？**  
A：检查是否正确点击了保存按钮，且页面顶部状态条显示「已保存」。保存成功后刷新落地协议页即可看到最新数据。

**Q：落地协议页定性义务列表是空的？**  
A：需要执行一次初始化操作（见部署说明第 8.5 节），这是部署后的必做步骤。

**Q：上传文件报"上传失败"？**  
A：检查文件大小是否超过 10MB，若是请先压缩文件再上传。

**Q：打开文件查看报错"无法打开"？**  
A：可能是文件本身损坏，或浏览器拦截了弹窗。请确认浏览器允许弹窗，或尝试点击「下载」按钮下载到本地打开。

**Q：忘记密码怎么办？**  
A：需要管理员在「用户管理」中重置密码。若连管理员密码也忘记，需要联系技术人员直接操作数据库重置。

**Q：想修改 KPI 目标值（例如协议重新谈判后数字变了）？**  
A：目标值目前写在代码配置文件中（`lib/agreement-config.js`），需要技术人员修改后重新部署。若需要支持在页面配置目标值，可作为后续需求提出。

**Q：数据大屏的折线图没有数据曲线？**  
A：折线图需要有月度数据才能画线。当有至少两个月的数据录入后，折线会自动出现。

---

*本文档描述系统截至 2026-07-22 的功能现状。如需新增功能，请在原需求基础上补充说明。*
